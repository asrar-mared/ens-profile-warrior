import { type RollupCommit, type RollupDeployment, AbstractRollup } from '../rollup.js';
import type { HexAddress, HexString, ProviderPair, ProofSequence } from '../types.js';
import { Contract } from 'ethers/contract';
import { EthProver } from '../eth/EthProver.js';
export type MorphConfig = {
    Rollup: HexAddress;
    poseidon: HexAddress;
};
export type MorphCommit = RollupCommit<EthProver> & {
    readonly l1BlockNumber: number;
};
export declare class MorphRollup extends AbstractRollup<MorphCommit> {
    static readonly mainnetConfig: RollupDeployment<MorphConfig>;
    static readonly holeskyConfig: RollupDeployment<MorphConfig>;
    readonly Rollup: Contract;
    readonly poseidon: HexAddress;
    constructor(providers: ProviderPair, config: MorphConfig);
    fetchLatestCommitIndex(): Promise<bigint>;
    protected _fetchParentCommitIndex(commit: MorphCommit): Promise<bigint>;
    protected _fetchCommit(index: bigint): Promise<MorphCommit>;
    encodeWitness(commit: MorphCommit, proofSeq: ProofSequence): HexString;
    windowFromSec(sec: number): number;
}
//# sourceMappingURL=MorphRollup.d.ts.map