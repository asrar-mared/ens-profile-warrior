const hasCallMetadata = (subject) => {
    if (typeof subject !== "object" || subject === null)
        return false;
    return ("__call_metadata" in subject &&
        typeof subject.__call_metadata === "object" &&
        subject.__call_metadata !== null);
};
export const getCall = (assertion, matcher) => {
    const subject = assertion._obj;
    if (!hasCallMetadata(subject))
        throw new TypeError(`You must provide a pending contract call to expect() when using .${matcher}, not '${typeof subject}'`);
    return subject;
};
//# sourceMappingURL=getCall.js.map