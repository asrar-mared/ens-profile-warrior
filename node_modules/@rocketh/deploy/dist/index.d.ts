import { Abi } from 'abitype';
import type { DeploymentConstruction, Deployment, Environment, LinkedData } from 'rocketh';
import { Address, Chain } from 'viem';
export type DeployResult<TAbi extends Abi> = Deployment<TAbi> & {
    newlyDeployed: boolean;
};
export type DeployFunction = <TAbi extends Abi, TChain extends Chain = Chain>(name: string, args: DeploymentConstruction<TAbi>, options?: DeployOptions) => Promise<DeployResult<TAbi>>;
export type DeployOptions = {
    linkedData?: LinkedData;
    deterministic?: boolean | `0x${string}` | {
        type: 'create2' | 'create3';
        salt?: `0x${string}`;
    };
    libraries?: {
        [name: string]: Address;
    };
} & ({
    skipIfAlreadyDeployed?: boolean;
} | {
    alwaysOverride?: boolean;
});
export declare function deploy(env: Environment): <TAbi extends Abi>(name: string, // '' allow to not save it
args: DeploymentConstruction<TAbi>, options?: DeployOptions) => Promise<DeployResult<TAbi>>;
//# sourceMappingURL=index.d.ts.map